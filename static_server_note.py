"""
Serve alert.html as a static file from the FastAPI app.
Add this import at the top of main.py after you verify everything works,
OR keep it separate — this module just patches the app to serve the HTML.

Usage: already wired into main.py via the /app route.
"""
# This is referenced by main.py — nothing to run here directly.
